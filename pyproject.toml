[project]
name = "gravatarmcp"
version = "0.1.0"
description = "A stdio-based MCP server for fetching Gravatar profiles"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
]
